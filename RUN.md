# Running

Since Next runs on port 3000 by default, the backend is configured to run on 5600, it can be changed in the env file.

### Backend


```bash
cd backend
pnpm install
pnpm dev
```

### Frontend


```bash
cd frontend
pnpm install
pnpm dev
```

